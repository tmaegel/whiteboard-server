// User
function User(sub, name, secret) {
    this.sub = sub;             /**< subject, id */
    this.name = name;           /**< name */
    this.secret = secret;       /**< description */
}

module.exports = User;
